# project-DS514-DS515
## 📌 ระบบการจำแนกประเภท (Classification System)

ส่วนนี้เป็นรายละเอียดของกระบวนการสร้างแบบจำลอง (Model) สำหรับการจำแนกระดับความเครียด  
โดยใช้ข้อมูลด้านพฤติกรรมและสุขภาพ เช่น ชั่วโมงการนอน ปริมาณคาเฟอีน อัตราการเต้นหัวใจ และข้อมูลด้านคุณภาพการนอน/ปัญหาสุขภาพ

โมเดลนี้แสดงขั้นตอนการพัฒนา AI แบบครบวงจร ได้แก่  
- การเตรียมข้อมูล  
- การทำ Feature Engineering  
- การสร้างโมเดล  
- การประเมินผล  
- การป้องกันปัญหา Label Leakage  

---

## 🚀 1. ภาพรวมของโปรเจกต์
โปรเจกต์นี้มีวัตถุประสงค์เพื่อสร้างโมเดล Machine Learning สำหรับทำนายว่า  
บุคคลนั้น **มีภาวะเครียดสูง (1)** หรือ **ภาวะปกติ (0)**

ฟีเจอร์สำคัญประกอบด้วย:

- Sleep Hours (ชั่วโมงการนอน)  
- Caffeine Intake (ปริมาณคาเฟอีน)  
- Heart Rate (อัตราการเต้นหัวใจ)  
- Sleep Quality (คุณภาพการนอน, ข้อมูลเชิงหมวดหมู่)  
- Health Issues (ภาวะสุขภาพ, ข้อมูลเชิงหมวดหมู่)

โมเดลหลักที่ใช้ คือ **Logistic Regression** พร้อมตัวเลือกเสริม เช่น  
- Standard Scaler  
- Polynomial Feature Expansion  
- Regularization (L2)

---

## 🧹 2. ขั้นตอนการเตรียมข้อมูล (Data Preprocessing)

### 🔹 ฟีเจอร์เชิงตัวเลข (Numerical)
- Sleep_Hours  
- Caffeine_mg  
- Heart_Rate  

ฟีเจอร์เหล่านี้ถูกแปลงด้วย **StandardScaler**  
เพื่อให้โมเดลเรียนรู้ได้ง่ายและค่า coefficient มีความหมายมากขึ้น

### 🔹 ฟีเจอร์เชิงหมวดหมู่ (Categorical)
- Sleep_Quality  
- Health_Issues  

ใช้ **OneHotEncoder** เพื่อแปลงเป็นตัวเลข

### 🔹 การป้องกัน Label Leakage
เพื่อลดปัญหาโมเดลแม่นเกินจริง จำเป็นต้องตรวจสอบว่า  
ฟีเจอร์ที่ใช้สร้าง label โดยตรง **ไม่ได้ถูกป้อนเข้าโมเดล** เช่น feature ที่ encode กติกา label มาโดยตรง

---

## ⚙️ 3. การสร้างฟีเจอร์ (Feature Engineering)

### 🔸 Polynomial Features (ตัวเลือกเสริม)
สร้างฟีเจอร์กำลังสองและฟีเจอร์แบบ interaction เช่น:

- Sleep_Hours²  
- Caffeine_mg²  
- Heart_Rate²  
- Sleep_Hours × Caffeine  
- Sleep_Hours × Heart Rate  

ข้อดี:
- ทำให้โมเดล Logistic Regression เรียนรู้ความสัมพันธ์แบบ “ไม่เชิงเส้น” ได้ดีขึ้น  
- ช่วยให้จับรูปแบบ เช่น จุดที่ความเครียดเพิ่มขึ้นแบบ non-linear หรือผลร่วมของหลายปัจจัย

---

## 🤖 4. การฝึกโมเดล (Model Training)

โมเดล Logistic Regression ใช้การตั้งค่า:

- `penalty='l2'` (ใช้ Regularization เพื่อป้องกัน overfitting)  
- `class_weight='balanced'` (เพื่อชดเชยปัญหา class imbalance)  
- `max_iter=1000–2000`  
- `C` เป็นตัวกำหนดความแรงของ regularization สามารถปรับแต่งได้ผ่าน GridSearchCV  

ตัวอย่าง Pipeline:

```python
pipeline = Pipeline([
    ('poly', PolynomialFeatures(degree=2, include_bias=False)),
    ('scaler', StandardScaler()),
    ('model', LogisticRegression(
        class_weight='balanced',
        max_iter=2000,
        random_state=42
    ))
])

pipeline.fit(X_train, y_train)

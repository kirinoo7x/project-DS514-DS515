# project-DS514-DS515
# 🧠 Stress Level Classification Model  
โปรเจกต์นี้เป็นระบบ Machine Learning สำหรับจำแนกระดับความเครียด (High Stress)  
โดยใช้ข้อมูลรายบุคคลด้านสุขภาพและพฤติกรรม เช่น ชั่วโมงการนอน ปริมาณคาเฟอีน และอัตราการเต้นหัวใจ  
โปรเจกต์นี้มุ่งเน้นทั้งการสร้างโมเดล การประเมินผล และการตรวจสอบปัญหา Label Leakage  
เพื่อให้โมเดลที่ได้สามารถใช้งานได้จริงและมีความน่าเชื่อถือ

---

## 🚀 1. วัตถุประสงค์ของโปรเจกต์
- สร้างโมเดล Machine Learning สำหรับจำแนกระดับความเครียด  
- ทดลองใช้เทคนิค preprocessing เช่น Scaling & Polynomial Features  
- ประเมินประสิทธิภาพของโมเดลด้วย metrics ที่ครอบคลุม  
- วิเคราะห์จุดผิดพลาด เช่น Label Leakage ซึ่งทำให้โมเดลแม่นผิดปกติ  
- สร้าง Pipeline ที่นำกลับมาใช้งานได้และขยายผลต่อได้ง่าย

---


## 🧹 2. การเตรียมข้อมูล (Data Preprocessing)

### 🔹 ฟีเจอร์เชิงตัวเลข (Numerical)
- Sleep_Hours  
- Caffeine_mg  
- Heart_Rate  

👉 ใช้ **StandardScaler** เพื่อทำให้โมเดลเรียนรู้ได้ดีขึ้นและ coefficient มีความหมายมากขึ้น

### 🔹 ฟีเจอร์เชิงหมวดหมู่ (Categorical)
- Sleep_Quality  
- Health_Issues  

👉 ใช้ **OneHotEncoder** สำหรับการแปลงเป็นตัวเลข หากข้อมูลไม่ได้ใช้สร้าง label โดยตรง\
---

## ⚙️ 3. การสร้างฟีเจอร์ (Feature Engineering)
ใช้ **PolynomialFeatures(degree=2)** เพื่อเพิ่มความสามารถของโมเดลในการเรียนรู้ความสัมพันธ์แบบไม่เชิงเส้น เช่น:

- Feature กำลังสอง: Sleep_Hours², Caffeine_mg²  
- Interaction: Sleep_Hours × Heart_Rate  

ข้อดี:  
- ช่วยให้โมเดล Logistic Regression ที่เป็นเชิงเส้นเรียนรู้ pattern ที่โค้งหรือซับซ้อนได้ดีขึ้น  

---

## 🤖 4. โมเดลที่ใช้ (Model)

ใช้ **Logistic Regression** ร่วมกับ:

- L2 Regularization  
- class_weight='balanced'  
- max_iter = 2000  
- StandardScaler  
- PolynomialFeatures  

### ตัวอย่าง Pipeline

```python
pipeline = Pipeline([
    ('poly', PolynomialFeatures(degree=2, include_bias=False)),
    ('scaler', StandardScaler()),
    ('logreg', LogisticRegression(
        class_weight='balanced',
        max_iter=2000,
        random_state=42
    ))
])
```

---

## 📊 5. การประเมินผลโมเดล (Evaluation)

ตัวชี้วัดที่ใช้วัดความแม่นยำ:

- Accuracy  
- Precision  
- Recall  
- F1-Score  
- Confusion Matrix  

ตัวอย่างผลลัพธ์:

```
precision    recall    f1-score
--------------------------------
0 (Normal)       1.00      1.00      1.00
1 (High Stress)  1.00      1.00      1.00
```

Confusion Matrix:

```
[[TN  FP]
 [FN  TP]]
```
---

## 🔧 6. การปรับ Hyperparameters

ใช้ **GridSearchCV** เพื่อเลือกค่าที่ดีที่สุด เช่น:

- Degree ของ PolynomialFeatures  
- ค่า Regularization (C) ของ Logistic Regression  

ตัวอย่าง parameter grid:

```python
param_grid = {
    'poly__degree': [1, 2, 3],
    'logreg__C': [0.01, 0.1, 1, 10]
}
```

---

## 📌 7. สรุปผลการทำงานของโมเดล

- Pipeline ถูกสร้างอย่างถูกต้อง: Scaling + Polynomial + Logistic Regression  
- โครงสร้างนี้สามารถต่อยอดไปใช้งานจริง หรือทดสอบกับข้อมูลจริงได้
- data set ที่ใช้มีความเข้าใจง่าย ไม่มี noise มีการแบ่งแยกกันชัดเจนส่งผลให้ model สามารถแบ่งออกจากกันได้ง่าย

---
